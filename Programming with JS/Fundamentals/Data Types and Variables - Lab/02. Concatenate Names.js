function concatenateNames(name1, name2, deliminator) {
  console.log(`${name1}${deliminator}${name2}`);
}

concatenateNames("John", "Smith", "->");
concatenateNames("Jan", "White", "<->");
concatenateNames("Linda", "Terry", "=>");
